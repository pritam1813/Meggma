{% comment %}
  Customer Reset Password Page
  Allows customers to reset their password.
{% endcomment %}

{% style %}
  .customer-reset-password {
    padding: 4rem 2rem;
    max-width: 480px;
    margin: 0 auto;
    min-height: 60vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .customer-reset-password__title {
    font-size: 2rem;
    font-weight: 600;
    text-align: center;
    margin-bottom: 0.5rem;
  }

  .customer-reset-password__subtitle {
    text-align: center;
    color: #666;
    margin-bottom: 2rem;
  }

  .customer-reset-password__form {
    background: #fff;
    border: 1px solid #e5e5e5;
    border-radius: 8px;
    padding: 2rem;
  }

  .customer-reset-password__field {
    margin-bottom: 1.5rem;
  }

  .customer-reset-password__label {
    display: block;
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }

  .customer-reset-password__input {
    width: 100%;
    padding: 0.75rem 1rem;
    border: 1px solid #d1d5db;
    border-radius: 6px;
    font-size: 1rem;
    transition: border-color 0.2s;
  }

  .customer-reset-password__input:focus {
    outline: none;
    border-color: #881337;
    box-shadow: 0 0 0 3px rgba(136, 19, 55, 0.1);
  }

  .customer-reset-password__submit {
    width: 100%;
    padding: 0.875rem 1.5rem;
    background: #881337;
    color: #fff;
    border: none;
    border-radius: 6px;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    transition: background-color 0.2s;
  }

  .customer-reset-password__submit:hover {
    background: #9f1239;
  }

  .customer-reset-password__errors {
    background: #fef2f2;
    border: 1px solid #fecaca;
    border-radius: 6px;
    padding: 1rem;
    margin-bottom: 1.5rem;
    color: #dc2626;
  }

  .customer-reset-password__errors li {
    list-style: disc;
    margin-left: 1.5rem;
  }
{% endstyle %}

<section class="customer-reset-password">
  <h1 class="customer-reset-password__title">{{ 'customer.reset_password.title' | t }}</h1>
  <p class="customer-reset-password__subtitle">{{ 'customer.reset_password.subtext' | t }}</p>

  <div class="customer-reset-password__form">
    {% form 'reset_customer_password' %}
      {% if form.errors %}
        <div class="customer-reset-password__errors">
          <ul>
            {% for field in form.errors %}
              <li>{{ form.errors.messages[field] }}</li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}

      <div class="customer-reset-password__field">
        <label for="password" class="customer-reset-password__label">
          {{ 'customer.reset_password.password' | t }}
        </label>
        <input
          type="password"
          id="password"
          name="customer[password]"
          class="customer-reset-password__input"
          autocomplete="new-password"
          required
        >
      </div>

      <div class="customer-reset-password__field">
        <label for="password_confirmation" class="customer-reset-password__label">
          {{ 'customer.reset_password.password_confirm' | t }}
        </label>
        <input
          type="password"
          id="password_confirmation"
          name="customer[password_confirmation]"
          class="customer-reset-password__input"
          autocomplete="new-password"
          required
        >
      </div>

      <button type="submit" class="customer-reset-password__submit">
        {{ 'customer.reset_password.submit' | t }}
      </button>
    {% endform %}
  </div>
</section>

{% schema %}
{
  "name": "Customer Reset Password",
  "settings": []
}
{% endschema %}
