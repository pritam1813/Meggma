<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    {% # Inlined CSS Variables %}
    {% render 'css-variables' %}

    {% comment %}
      Font preloading:
      1. Preconnect to font CDN for faster connection
      2. Preload only the critical font variant (base weight)
      3. Additional variants load on-demand via @font-face
    {% endcomment %}
    {% unless settings.type_primary_font.system? %}
      {% if settings.type_primary_font.family %}
        <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>

        {% comment %} Preload the base font variant for initial page render {% endcomment %}
        {{ settings.type_primary_font | font_url | preload_tag: as: 'font', crossorigin: 'anonymous' }}
      {% endif %}
    {% endunless %}

    {% comment %}
      Logo preload is handled via the header section's render.
      The header section outputs a preload tag using section.settings.
      See sections/header.liquid for the implementation.
    {% endcomment %}

    {% # Load and preload the critical CSS %}
    {{ 'color-palette.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'button-styles.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'critical.css' | asset_url | stylesheet_tag: preload: true }}
    {{ 'tailwind.output.css' | asset_url | stylesheet_tag: preload: true }}

    {% # Social, title, etc. %}
    {% render 'meta-tags' %}

    {{ content_for_header }}
  </head>

  <body class="bg-settings text-settings font-primary">
    <a
      class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 z-[9999] px-6 py-3 bg-white text-black border border-black"
      href="#main-content"
    >
      {{ 'general.accessibility.skip_to_content' | t }}
    </a>
    {% sections 'header-group' %}

    {%- comment -%}
      Main content wrapper with automatic padding for non-homepage pages.
      Homepage templates handle their own hero sections that should overlap the header.
    {%- endcomment -%}
    <main
      id="main-content"
      {% unless template.name == 'index' %}
        class="pt-24 sm:pt-28 lg:pt-32"
      {% endunless %}
    >
      {{ content_for_layout }}
    </main>

    {% sections 'footer-group' %}

    <script src="{{ 'gsap.min.js' | asset_url }}" defer></script>
    <script src="{{ 'ScrollTrigger.min.js' | asset_url }}" defer></script>
    <script src="{{ 'Draggable.min.js' | asset_url }}" defer></script>
  </body>
</html>
